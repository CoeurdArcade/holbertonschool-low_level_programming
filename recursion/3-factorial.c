#include "main.h"

/**
 * int_factorial - function that returns the factorial given digit
 * @n: given number
 *
 * Return: factorial recursives
 */

int_factorial(int n)
{
	n = 0;
	{
	if (n != 0)
	return (-1);
	return (n, "%d", n++)
	}
}
