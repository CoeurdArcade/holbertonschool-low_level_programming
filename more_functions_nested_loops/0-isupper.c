#include "main.h"
#include <stdio.h>

/**
 * i_isupper - is a function that check for uppercase character
 * @c: the character to check
 *
 * Return: 1 if c is uppercase 0 otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
