#include "main.h"
#include "stdio.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: character to check
 *
 * Return: value 0 if >= 0
 * 	value - if < 0
 */

int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
